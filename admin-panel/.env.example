# KhabarExpress Admin Panel Environment Variables

# Backend API base URL (include /api/v1 prefix)
# In development, the Vite proxy handles this so you can leave it as /api/v1
# In production, set to your backend's full URL e.g. https://api.khabarexpress.com/api/v1
VITE_API_URL=/api/v1
